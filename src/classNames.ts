 
import {
    
} from '@parrotjs/rc-utils-whichtype';

export default function classNames(...args){
    let classes=[];
    for(let i=0;i<arguments.length;i++){
        let arg = arguments[i];

        if(arg)
    }
}   